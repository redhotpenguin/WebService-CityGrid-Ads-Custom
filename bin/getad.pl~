#!/usr/bin/perl

use strict;
use warnings;

use WebService::CityGrid::Ads::Custom;

my $Cg = WebService::CityGrid::Ads::Custom->new({ publisher => 5416626221 });

my $res = $cg->query({ what => 'burger', where => '94109' });

use Data::Dumper;
warn Dumper($res);
